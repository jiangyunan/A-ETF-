# ETF 动量轮动系统 — 实盘操作命令

## 概述

本技能提供实盘交易环节中两个核心命令的使用方法：
- `python main.py --signal` — 生成本周持仓建议
- `python main.py --log` — 命令行快捷录入实际交易

## 命令详解

### 1. `python main.py --signal` — 本周实盘持仓建议

#### 用途
基于最新行情数据，生成本周应该持有的 ETF 及目标权重。

#### 语法
```bash
python main.py --signal
```

#### 执行流程
1. 自动拉取最新 K 线数据（从本地 Parquet 缓存增量更新）
2. 计算风险调整动量排名
3. 市场状态机判断牛/震/熊
4. 应用风险预算约束（逆波动率权重、单只上限 25%、大类上限 40%）
5. 输出本周持仓建议

#### 输出示例
```
============================================================
  ETF 动量轮动 V2 — 本周实盘信号
============================================================

正在拉取最新行情...
      数据更新至: 2025-05-08
      实时溢价: 19 只 ETF

============================================================
  信号日期: 2025-05-08
  市场状态: ⚔️ 牛市 (集中)
  策略: 状态机=开 相关过滤=开 波动率控仓=开
  建议持仓: 3 只 ETF
============================================================
  代码       名称                     动量得分        权重
  ------------------------------------------
  512760   芯片ETF               12.7081    33.3%
  513500   标普500ETF            12.6437    22.8%
  159915   创业板ETF              10.2562    33.3%
============================================================

  📅 执行日: 下一交易日（信号日+1）
  下次更新: 下周一收盘后运行 python main.py --signal
```

#### 输出字段说明

| 字段 | 含义 |
|------|------|
| 信号日期 | 最新 K 线日期（基于最新可用数据） |
| 市场状态 | 牛市（集中3只）/ 震荡（分散5只）/ 熊市（防御资产） |
| 代码 | ETF 代码 |
| 动量得分 | 风险调整后的动量值（越高越强） |
| 权重 | 经风险预算约束后的目标仓位（已归一化） |

#### 实盘执行规则

| 场景 | 信号日 | 执行日 |
|------|--------|--------|
| 普通周 | 周一收盘 | 周二开盘 |
| 周一休市 | 周二收盘 | 周三开盘 |
| 长假（≥3天休市） | 开市第1天收盘 | 第2天开盘 |

#### 使用时机
- 每周一/第一个交易日收盘后运行
- 看到信号后，对照当前持仓决定调仓

---

### 2. `python main.py --log` — 命令行快捷录入

#### 用途
将实际交易一笔写入 SQLite 数据库，无需进入交互面板。

#### 语法
```bash
python main.py --log <BUY/SELL> <代码> <价格> <股数> [溢价%] [备注]
```

#### 参数

| 参数 | 必需 | 说明 | 示例 |
|------|------|------|------|
| BUY/SELL | ✅ | 买入或卖出 | `BUY` |
| 代码 | ✅ | ETF 代码 | `513100` |
| 价格 | ✅ | 成交价 | `1.250` |
| 股数 | ✅ | 成交股数 | `1000` |
| 溢价% | ❌ | 买入时溢价率 | `1.2%` |
| 备注 | ❌ | 自由文本 | `按信号买入` |

#### 示例

```bash
# 买入纳指ETF
python main.py --log BUY 513100 1.250 1000 1.2% 按信号买入

# 卖出沪深300
python main.py --log SELL 510300 1.150 500

# 买入芯片ETF（无溢价）
python main.py --log BUY 512760 1.180 800
```

#### 输出
```
✅ 已录入: BUY 513100 纳指ETF @1.250 ×1000 = ¥1250
```

#### 数据存储
所有交易写入 `ops/trade_log.db`（SQLite），字段包括：
- date, action, code, name, price, shares
- premium_pct（溢价率，自动解析 `1.2%` → `0.012`）
- signal_date（信号日期）
- state（市场状态）
- notes（备注）

#### 后续查看
```bash
# 查看交易统计
python main.py --track

# 打开交互面板（含持仓对比）
python main.py --trade-ui
```

---

## 典型工作流

```bash
# 1. 每周一收盘后：获取本周信号
python main.py --signal

# 2. 周二执行：买入/卖出对应 ETF
python main.py --log BUY 513100 1.250 1000 1.2% 按信号买入
python main.py --log SELL 510300 1.150 500
python main.py --log BUY 512760 1.180 800

# 3. 随时查看统计
python main.py --track

# 4. 盘前检查 + 风险告警
python main.py --preflight
python main.py --alerts
```

## 常见问题

**Q: --signal 显示的日期是上周的怎么办？**
确保已运行 `python main.py`（触发数据拉取），再运行 `--signal`。数据缓存为 Parquet 格式。

**Q: --log 写入后如何验证？**
运行 `python main.py --track` 查看 5 维度统计面板，包含已实现盈亏、滑点、溢价影响等。

**Q: 如何同时看到操作建议和当前持仓对比？**
运行 `python main.py --trade-ui` 进入交互面板，自动显示信号 vs 持仓差异。
